name: API Tests with JWT

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  install:
    name: Install
    uses: ./.github/workflows/job-install.yml
  test-unit:
    name: Test-Unit
    needs: install
    uses: ./.github/workflows/job-test-unit.yml
